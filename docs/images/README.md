# docs/images

Screenshots and renders referenced from the top-level README, e.g.:

- `rviz_overview.png` — RViz showing the robot, TF frames, wrist point cloud
  and/or octomap overlay
- `gazebo_world.png` — the Gazebo world model (podium + table + robot)

Add images here with these names (or update the README's `<img>` tags if you
name them differently).
